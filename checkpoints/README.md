# Checkpoints

Place uploaded checkpoints here. This directory is intentionally empty in the
code bundle.

Preserve the checkpoint subdirectory names expected by the task scripts, for
example `Cosmos-Predict1-4B/` or
`satellite_tokenizer/agri/<run>/checkpoints/`.
